export interface ISalepoint {
  sp_id: number,
  field_code: string,
  name: string,
  geo_id: number,
  cap: string,
  comune: string,
  province: string,
  region: string,
  tel: string,
  potential: number,
  coords: string
}
